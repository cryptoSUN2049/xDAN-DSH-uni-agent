# VERL 线每日训练日记

每天一节，最新的一天在最上面。只写当天的结论和数字。逐条细节在 `tasks/memory.md`（台账），方案在 `训练方案.md` 和 `opd-then-rl-design.md`，排期在 `gpu-schedule.md`。
时间一律用 UTC。数字写到能复算的程度：区间、样本量、步数、提交号。

每节固定七项：一句话、训练进度、关键结果、决策、问题与修复、资源与费用、下一步。

---

## 2026-09-19（周六）

**一句话**：联合训练 S1 显著退步（−0.172）。严查确认 OPD 打分的实现没有问题，问题在算法层面：老师信号主要落在说明文字上，并且推长篇幅。之后按负责人决定改跑"先 OPD 后 RL"（pipe-s2），并交给守护进程保证跑完。

**训练进度**

| run | 做法 | 状态 |
|---|---|---|
| pipe-s1（S1） | 联合：RL + 1.0 × OPD，8 题 × 4，学习率 1e-4 | 停在第 12 步，04:50 暂停，不再继续 |
| pipe-s2 阶段 A | 纯 OPD 12 步 | 06:08 开始，**12:41 完成**；第 12 步回复长度 2.52 万，蒸馏损失 0.066 |
| pipe-s2 阶段 B | 纯 RL，第 13–60 步，只加载权重，优化器重新开始 | 13:00 开始训练，每步约 31–36 分钟，预计 09-20 13:00–16:00 完成；第 13 步 8 组全是全对或全错，梯度为 0，不是 bug |

**关键结果**

- **S1 第 12 步快检**（58 题 × 2，与原版配对）：−0.172 [−0.285, −0.060]，退步。
  - SWE −0.232，显著；Terminal-Lego −0.117，不显著。
  - 输出 token 中位数 +48%；SWE 跑满轮数从 48% 升到 68%；超时比例翻倍。
- **老师打分审计**（第 12 步的 29 条真实轨迹，约 78 万个位置）：
  - 实现层全部通过：位置错位 0；生产打分与 HF 的平均差 0.0074，属于 bf16 噪声；前缀缓存和分块不引入偏差；VERL 的切片和 k1 函数与手工推导完全一致。
  - 格式层：学生不思考，网关拼出的序列与老师原生格式逐 token 一致（818 个 token）。
  - 算法层：
    - k1 有 42.5% 落在 analysis、21.3% 落在 plan，命令只占 30.6%，结束决策约为 0。
    - 句末收尾的 `.",` 平均 k1 为 +0.134，每次收尾都被扣分，导致篇幅变长。
    - 8 组里有 6 组全对或全错，这些组上只有 OPD 信号。
- **pipe-s2 阶段 A**：
  - 蒸馏损失从 0.108 降到约 0.055。
  - 回复长度从 1.62 万升到 2.0–2.6 万 token，撞上限比例 3.1%–3.4%。
- **他线参照（Tinker）**：
  - 只用 RL 的 A'：全量 +0.199 [+0.115, +0.282]。
  - 先 OPD 8 步再 RL 24 步：与原版比 −0.160 [−0.247, −0.077]，与 A' 比 −0.359。
  - Tinker 线已决定主线只用 RL。

**决策（负责人）**

- 03:00：S1 带老师跑完，随后被 04:05 的快检结果推翻。
- 04:50：暂停 S1，先查老师打分格式。
- 05:40：训练路线定为先 OPD 后 RL。
- 07:55："先完整训练完再说"，pipe-s2 不改动，中途不设止损。
- 08:10：Modal 上限调到 900 美元。
- 07:53：批准磁盘第 1 档清理。
- 12:00：TB2.1 原版评测每题 3 次。

**问题与修复**

- e178270：文件记录器改为每次尝试一个文件，续跑不再覆盖第 1–12 步；控制台缺行时从这些文件取逐步指标。已在生产中验证。
- 613aab9：
  - 新增 `run_opd_then_rl.sh`（分阶段训练）和 `CKPT_LOAD_CONTENTS`；
  - 新增老师格式守卫：带老师启动前逐 token 比对格式，在真实分词器上已通过；
  - 新增 `diag_teacher_scoring.py`。
- 守护进程（`supervise_run.sh`）：驱动退出时，只要第 60 步没完成就自动续跑，最多 3 次；续跑前等评测进程让出，最多等 14 小时。
- 更正：我 04:50 说"老师格式不一致"是错的（那次用了开思考的渲染）。已记入 lessons 53。
- 同步源码时 SSH 断开，VERL 补丁一度缺失，已重打并确认。已记入 lessons 52。
- 阶段 A 的 delta 阶段挑中了轮转后留下的空壳 checkpoint 而失败。已修复：改为挑选最早的、有模型文件的 checkpoint，附 3 个测试，已部署。训练本身不受影响。

**资源与费用**

- Modal 本月累计：585.95（05:27）→ 624.67 美元（08:10）；上限 900。S1 约 3 美元/小时，每步约 1.5 美元。
- 磁盘：清理约 146 GB（旧 run 的 checkpoint、失败的合并产物、4B 模型的本地副本、uv 临时目录）；容器盘占用从 80% 降到 73%。
- 本轮只保留最近 3 个 checkpoint，另外永久保留第 20、40、60 步，峰值约 175 GB。

**下一步**

- 约 12:40–14:10：阶段 A 第 12 步快检。
- 快检约 17:00 结束，之后 GPU1 跑 eval-set-v1 原版 78 × 4（约 21:00–22:00）。TB2.1 原版挪到 09-20 5e 全量结束后，以免挤掉 5e 01:00 的窗口。
- 09-20 08:30：pipe-s2 完成，然后跑最终模型的全量评测，与原版配对。
- 待办：修改 `run_opd_round.sh` 的孤儿进程清理，跳过外部 vLLM（`KEEP_GPU_PROCESS=1`），需要在 Tinker 评测会话 09-20 01:00 使用 GPU1 之前完成。

---

## 2026-09-18（周五）

**一句话**：发现 pass_ratio 奖励存在"什么都不做也能拿高分"的下限，改用二值奖励；pipe-r11 修掉补齐样本导致的崩溃后验收通过；随后启动 S1。

**训练进度**

- pipe-r9：06:07 停止。原因是 SWE 部分分的下限中位数达到 0.936。
- pipe-r11：二值奖励，20 步，18:23 验收 PASS。
- S1：19:26 启动。

**关键结果**

- pipe-r11：费用 26.83 美元，计费/实际用量比 1.02；训练集前后两半的解决率 0.553 / 0.544，看不出学习信号；长度无副作用。
- S1 训练前验证 13/32：Terminal-Lego 12/16，SWE 1/16。

**决策**

- 默认奖励改为二值。
- S1 带老师、用阶梯 A、双卡启动。

**问题与修复**

- pipe-r11 第 5 步崩溃：VERL 补齐占位样本时没有重建老师的对数概率。修复为 `patches/verl/0001`，并在 S1 第 2 步的生产环境中验证。
- 失败处理：一组有效轨迹不少于 `MIN_VALID_SESSIONS`（组大小的一半）就保留。
- cost 阶段改为按小时查账单，并用 Sandbox 单价。

**资源与费用**：Modal 当日约 187 美元，两条线合计。

**下一步**：S1 跑满 60 步（后来改变）。

---

## 2026-09-17（周四）

**一句话**：路线 ②（真老师蒸馏）全流程打通，pipe-r4 验收 PASS；共享评估集 eval-set-v1 冻结；Modal 换了工作区。

- pipe-r2（4B 纯 RL）、pipe-r3（4B 自评老师，蒸馏损失约 0，作为零点校验）、pipe-r4（9B 学生 + 27B 老师）全部 PASS。pipe-r4 的蒸馏损失为 0.10–0.13，确认真老师的信号进来了；held-out 0.988 → 0.914（只有 7 题，已饱和）。
- 4B 在 TB2.1 上：0/66（另有 23 题因基础设施问题未完成）。
- Modal 旧工作区三次撞到上限，换到 l98348740。当日约 365 美元。
