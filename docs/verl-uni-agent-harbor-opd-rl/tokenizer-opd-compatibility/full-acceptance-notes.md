# 本轮执行与边界记录

- 真实训练使用新目录 `runs/opd-live-acceptance-20260924`，原16步训练与证据不修改。
- 只读 hook 包装原生 loss，保留后续 backward；CPU dense/nested fixture 通过，真实训练另行验收。
- 独立审查要求：8条完整覆盖、连续microbatch编号、明确训练退出码和checkpoint、非空mask、actor温度0.8。
- 教师现场logprob必须额外与独立HF@1逐token对照，不能只用同一输入重算loss。
- 两模型原生thinking模板有差异，按学生模板的原始IDs评分；模板不同不等于坐标错误。
- 工具mask测试使用真实ToolAgentLoop方法与parser，但生成传输受控；不冒充自主工具能力。
- 显存预分配不代表权重加载完成，加载状态以分片与引擎完成日志为准。
- 所有模型任务按训练→导出重载→模式生成→HF评分→vLLM评分串行执行。

## 后续产线建设（不混入本次兼容验收）

1. 将raw token IDs、loss mask、teacher评分摘要与模型/数据版本纳入标准trace；仅保留decode文本不足以精确回溯。
2. 原生k1路径只检查teacher IDs形状，生产准入可增加有效位置逐ID相等断言。
3. 固定验证集与封存测试分离；现30题已反复观测，不能冒充封存泛化测试。
4. 代码需要隔离执行测试、聊天需要独立质量标准，再定义跨领域回归/自动止损；当前不能以语法通过率或无分数作为性能通过。
5. teacher T1/student T0.8 是实际非等温目标，报告与配置必须明确，不能泛称同温KL。
6. thinking评分坐标兼容不等于推理质量已对齐；还需正确率、思考闭合、token预算和截断率的固定任务评测。
